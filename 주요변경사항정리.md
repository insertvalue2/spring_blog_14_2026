# 주요 변경사항 정리
## 14단계: 비밀번호 암호화 (BCrypt) 구현

### 개요
Spring Security Crypto 모듈의 BCryptPasswordEncoder를 활용하여 비밀번호를 안전하게 암호화하여 저장하도록 구현했습니다. 사용자가 입력한 평문 비밀번호는 BCrypt 알고리즘으로 해싱되어 데이터베이스에 저장되며, 로그인 시에는 암호화된 비밀번호와 평문 비밀번호를 안전하게 비교합니다.

**주요 특징**:
- BCrypt 단방향 해싱 알고리즘 사용
- Salt 자동 생성 (매번 다른 해시값 생성)
- 비밀번호 복호화 불가능 (보안 강화)
- 안전한 비밀번호 검증 (matches 메서드 사용)

---

## 📋 목차 (빠른 이동)

- [1. 전체 흐름 한눈에 보기](#1-전체-흐름-한눈에-보기)
- [2. 의존성 추가 (build.gradle)](#2-의존성-추가-buildgradle)
- [3. PasswordEncoder 빈 등록 (WebMvcConfig)](#3-passwordencoder-빈-등록-webmvcconfig)
- [4. 회원가입 시 비밀번호 암호화 (UserService)](#4-회원가입-시-비밀번호-암호화-userservice)
- [5. 로그인 시 비밀번호 검증 (UserService)](#5-로그인-시-비밀번호-검증-userservice)
- [6. 회원정보 수정 시 비밀번호 암호화 (UserService)](#6-회원정보-수정-시-비밀번호-암호화-userservice)
- [7. Repository 쿼리 수정 (UserRepository)](#7-repository-쿼리-수정-userrepository)
- [💡 학생들을 위한 개념 설명](#-학생들을-위한-개념-설명)

---

## 1. 전체 흐름 한눈에 보기

### 회원가입 흐름
```
1. 사용자가 회원가입 폼에 비밀번호 입력 (평문)
   ↓
2. UserService.회원가입() 메서드 실행
   ↓
3. passwordEncoder.encode(평문비밀번호) 호출
   ↓
4. BCrypt로 암호화된 해시값 생성 (예: $2a$10$...)
   ↓
5. 암호화된 비밀번호를 DB에 저장
```

### 로그인 흐름
```
1. 사용자가 로그인 폼에 비밀번호 입력 (평문)
   ↓
2. UserService.로그인() 메서드 실행
   ↓
3. 사용자명으로 사용자 조회 (암호화된 비밀번호 포함)
   ↓
4. passwordEncoder.matches(평문비밀번호, 암호화된비밀번호) 호출
   ↓
5. BCrypt가 자동으로 Salt를 고려하여 비교
   ↓
6. 일치하면 로그인 성공, 불일치하면 로그인 실패
```

---

## 2. 의존성 추가 (build.gradle)

**파일**: `build.gradle`

```gradle
dependencies {
    // ... 기존 의존성 ...
    
    // 암호화
    implementation 'org.springframework.security:spring-security-crypto'
    
    // ... 기존 의존성 ...
}
```

**핵심 포인트**:
- Spring Security Crypto 모듈은 Spring Security의 일부이지만, 전체 Spring Security를 사용하지 않고 암호화 기능만 사용할 수 있습니다.
- BCryptPasswordEncoder를 사용하기 위한 최소한의 의존성입니다.

**Gradle 새로고침**:
- 의존성 추가 후 IDE에서 Gradle 프로젝트를 새로고침하거나 `./gradlew build --refresh-dependencies` 실행

---

## 3. PasswordEncoder 빈 등록 (WebMvcConfig)

**파일**: `src/main/java/org/example/demo_ssr_v1_1/_core/config/WebMvcConfig.java`

```java
@Configuration
@RequiredArgsConstructor
public class WebMvcConfig implements WebMvcConfigurer {
    
    // ... 기존 코드 ...
    
    /**
     * 비밀번호 암호화를 위한 PasswordEncoder 빈 등록
     * 
     * BCryptPasswordEncoder는 Spring Security Crypto 모듈에서 제공하는
     * 비밀번호 해싱 알고리즘입니다.
     * 
     * 특징:
     * - BCrypt 알고리즘 사용 (단방향 해싱)
     * - Salt 자동 생성 (매번 다른 해시값 생성)
     * - 비밀번호 검증: matches() 메서드 사용
     * 
     * @return BCryptPasswordEncoder 인스턴스 (싱글톤)
     */
    @Bean // IoC 대상 - 싱글톤 처리
    public PasswordEncoder passwordEncoder() {
        return new BCryptPasswordEncoder();
    }
}
```

**필요한 임포트**:
```java
import org.springframework.context.annotation.Bean;
import org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder;
import org.springframework.security.crypto.password.PasswordEncoder;
```

**핵심 포인트**:
- `@Bean` 어노테이션으로 IoC 컨테이너에 빈으로 등록
- 싱글톤 패턴으로 관리되어 어디서든 동일한 인스턴스 사용
- UserService에서 의존성 주입으로 사용 가능

---

## 4. 회원가입 시 비밀번호 암호화 (UserService)

**파일**: `src/main/java/org/example/demo_ssr_v1_1/user/UserService.java`

### 4-1. PasswordEncoder 주입

```java
@Service
@RequiredArgsConstructor
public class UserService {

    private final UserRepository userRepository;
    // 비밀번호 암호화를 위한 PasswordEncoder 주입
    private final PasswordEncoder passwordEncoder;
    
    // ... 기존 코드 ...
}
```

### 4-2. 회원가입 메서드 수정

```java
@Transactional
public User 회원가입(UserRequest.JoinDTO joinDTO) {
    // 1. 유효성 검사
    joinDTO.validate();

    // 2. 사용자명 중복 체크
    if (userRepository.findByUsername(joinDTO.getUsername()).isPresent()) {
        throw new Exception400("이미 존재하는 사용자 이름입니다");
    }

    // 3. 프로필 이미지 저장 (선택사항)
    // ... 프로필 이미지 처리 코드 ...

    // 4. 비밀번호 암호화 처리
    // 회원가입 요청자가 제출한 password를 BCrypt로 암호화
    // BCrypt는 단방향 해싱 알고리즘이므로 복호화 불가능
    // 검증 시에는 passwordEncoder.matches() 메서드 사용
    String hashPwd = passwordEncoder.encode(joinDTO.getPassword());
    
    // 5. DTO를 엔티티로 변환 (암호화된 비밀번호와 파일명 포함)
    User user = joinDTO.toEntity(profileImageFilename);
    // 암호화된 비밀번호로 교체
    user.setPassword(hashPwd);

    // 6. 기본 권한 처리 (일반 사용자)
    // 주의: User 엔티티의 빌더(생성자)에서 roles 리스트가 비어 있으면
    // 자동으로 USER 권한을 1개 추가하도록 설계되어 있습니다.
    // 따라서 회원가입 시 Service 레이어에서 addRole(Role.USER)를
    // 한 번 더 호출하면 user_role_tb에 중복(예: user_id=11, role=USER)이 발생하여
    // 유니크 제약조건 오류가 납니다.
    //
    // → 결론: 기본 USER 권한은 User 엔티티가 책임지고 처리하므로
    //         회원가입 서비스에서는 별도의 addRole 호출을 하지 않습니다.

    // 7. JpaRepository의 save() 메서드: 엔티티 저장 (INSERT)
    return userRepository.save(user);
}
```

**핵심 포인트**:
- `passwordEncoder.encode()`: 평문 비밀번호를 BCrypt 해시값으로 변환
- 암호화된 비밀번호는 `$2a$10$...` 형식의 문자열로 저장됨
- 같은 비밀번호를 암호화해도 매번 다른 해시값이 생성됨 (Salt 자동 생성)

**필요한 임포트**:
```java
import org.springframework.security.crypto.password.PasswordEncoder;
```

---

## 5. 로그인 시 비밀번호 검증 (UserService)

**파일**: `src/main/java/org/example/demo_ssr_v1_1/user/UserService.java`

### 5-1. 로그인 메서드 수정

```java
@Transactional(readOnly = true)
public User 로그인(UserRequest.LoginDTO loginDTO) {
    // 1. 유효성 검사
    loginDTO.validate();

    // 2. 사용자명으로 사용자 조회 (+ 역할 정보까지 함께 조회)
    //    findByUsernameWithRoles():
    //    - User 엔티티와 roles 컬렉션을 LEFT JOIN FETCH로 한 번에 가져옵니다.
    //    - 비밀번호는 DB에서 직접 비교하지 않고 애플리케이션에서 검증
    User user = userRepository.findByUsernameWithRoles(loginDTO.getUsername())
            .orElse(null);

    // 3. 사용자가 존재하지 않으면 로그인 실패
    if (user == null) {
        throw new Exception400("사용자명 또는 비밀번호가 올바르지 않습니다");
    }

    // 4. 비밀번호 검증 (BCrypt matches 메서드 사용)
    // passwordEncoder.matches(평문비밀번호, 암호화된비밀번호)
    // - 사용자가 입력한 평문 비밀번호와 DB에 저장된 암호화된 비밀번호를 비교
    // - BCrypt 알고리즘이 자동으로 Salt를 고려하여 비교
    // - 일치하면 true, 불일치하면 false 반환
    if (!passwordEncoder.matches(loginDTO.getPassword(), user.getPassword())) {
        throw new Exception400("사용자명 또는 비밀번호가 올바르지 않습니다");
    }

    // 5. 로그인 성공: 사용자 엔티티 반환
    return user;
}
```

**핵심 포인트**:
- `passwordEncoder.matches()`: 평문 비밀번호와 암호화된 비밀번호를 안전하게 비교
- BCrypt가 자동으로 Salt를 추출하여 비교하므로 개발자가 직접 처리할 필요 없음
- 사용자명과 비밀번호를 모두 확인하여 보안 강화

---

## 6. 회원정보 수정 시 비밀번호 암호화 (UserService)

**파일**: `src/main/java/org/example/demo_ssr_v1_1/user/UserService.java`

```java
@Transactional
public User 회원정보수정(UserRequest.UpdateDTO updateDTO, Long userId) {
    // 1. 수정하려는 회원정보 조회
    User user = userRepository.findById(userId)
            .orElseThrow(() -> new Exception404("사용자를 찾을 수 없습니다"));

    // 2. 인가 검사: 자기 자신의 정보만 수정 가능한지 확인
    if (!user.isOwner(userId)) {
        throw new Exception403("회원정보 수정 권한이 없습니다");
    }

    // 3. 유효성 검사
    updateDTO.validate();

    // 4. 비밀번호 암호화 처리
    // 회원정보 수정 시 입력한 비밀번호를 BCrypt로 암호화
    String hashPwd = passwordEncoder.encode(updateDTO.getPassword());
    // DTO의 비밀번호를 암호화된 비밀번호로 교체
    updateDTO.setPassword(hashPwd);

    // 5. 프로필 이미지 처리 (새로운 이미지가 업로드된 경우)
    // ... 프로필 이미지 처리 코드 ...

    // 6. 더티 체킹을 활용한 수정 처리
    user.update(updateDTO);

    // 7. 변경된 엔티티 저장 (더티 체킹)
    User updateUser = userRepository.save(user);

    // 8. 수정된 사용자 정보 반환 (Controller에서 세션 갱신용)
    return updateUser;
}
```

**핵심 포인트**:
- 회원정보 수정 시에도 비밀번호를 암호화하여 저장
- 기존 비밀번호와 비교하지 않고 새로운 비밀번호를 암호화하여 저장

---

## 7. 카카오 소셜 로그인 임시 비밀번호 암호화 (UserController)

**파일**: `src/main/java/org/example/demo_ssr_v1_1/user/UserController.java`

### 7-1. PasswordEncoder 주입

```java
@RequiredArgsConstructor
@Controller
public class UserController {

    // Service 레이어 주입
    private final UserService userService;
    // 소셜 로그인 사용자의 임시 비밀번호도 동일하게 해싱 처리하기 위해 주입
    private final PasswordEncoder passwordEncoder;

    @Value("${oauth.kakao.client-id}")
    private String kakao_client_id;

    @Value("${tenco.key}") // 임시 비밀번호 키 주입
    private String tencoKey;

    // ...
}
```

### 7-2. 소셜 로그인 최초 가입 시 임시 비밀번호 해싱 처리

```java
if (userOrigin == null) {
    System.out.println("4. 기존 회원이 아니므로 자동 회원가입을 진행합니다.");

    // 회원가입용 엔티티 생성
    User newUser = User.builder()
            .username(username)
            // 임시 비밀번호도 일반 회원가입과 동일하게 BCrypt 해싱 처리
            .password(passwordEncoder.encode(tencoKey)) // 임시 비밀번호 (DB Not Null 제약 대응, 해싱 저장)
            .email(username + "@kakao.com") // 임의의 이메일 (선택사항)
            .provider(OAuthProvider.KAKAO) // ★ 로그인 경로 설정
            .build();

    // 프로필 이미지가 있다면 설정
    String profileImage = kakaoProfile.getProperties().getThumbnailImage();
    if (profileImage != null && !profileImage.isEmpty()) {
        newUser.setProfileImage(profileImage); // URL 그대로 저장 (외부 링크)
    }

    userService.소셜회원가입(newUser);
    userOrigin = newUser; // !! 필수
}
```

**핵심 포인트**:
- 소셜 로그인 사용자는 실제로 비밀번호를 사용하지 않지만, DB `password` 컬럼이 NOT NULL 이므로 임시 값이 필요합니다.
- 이 임시 비밀번호도 일반 회원가입과 동일하게 BCrypt로 해싱하여 저장합니다.
- 로그인 시에는 소셜 로그인은 username(예: `닉네임_카카오ID`)으로만 처리하므로, 해싱된 임시 비밀번호가 로그인 흐름에 영향을 주지 않습니다.

---

## 8. Repository 쿼리 수정 (UserRepository)

**파일**: `src/main/java/org/example/demo_ssr_v1_1/user/UserRepository.java`

### 7-1. 기존 쿼리 메서드 수정

```java
/**
 * 로그인 시 역할(ROLE) 정보까지 함께 조회하는 메서드.
 *
 * - LEFT JOIN FETCH 를 사용하여 user_role_tb 를 한 번에 로딩합니다.
 * - 세션에 저장된 User 객체에서 isAdmin(), getRoleDisplay() 등을 사용할 수 있습니다.
 * 
 * 비밀번호 검증:
 * - 비밀번호는 DB에서 직접 비교하지 않고 애플리케이션에서 검증합니다.
 * - BCrypt로 암호화된 비밀번호는 평문과 직접 비교할 수 없으므로
 * - Service 레이어에서 passwordEncoder.matches() 메서드로 검증합니다.
 */
@Query("select distinct u from User u left join fetch u.roles r " +
       "where u.username = :username")
Optional<User> findByUsernameWithRoles(@Param("username") String username);
```

**변경 사항**:
- 기존: `findByUsernameAndPasswordWithRoles()` (비밀번호 조건 포함)
- 변경: `findByUsernameWithRoles()` (비밀번호 조건 제거)

**핵심 포인트**:
- 비밀번호는 DB에서 직접 비교하지 않음
- 사용자명만으로 조회한 후 애플리케이션에서 비밀번호 검증
- BCrypt로 암호화된 비밀번호는 평문과 직접 비교 불가능

### 7-2. 사용하지 않는 메서드 처리

```java
/**
 * 사용자명과 비밀번호로 사용자 조회 (로그인용)
 * 
 * ⚠️ 주의: 비밀번호 암호화 적용 후 더 이상 사용하지 않습니다.
 * - BCrypt로 암호화된 비밀번호는 평문과 직접 비교할 수 없습니다.
 * - 로그인 시에는 findByUsernameWithRoles()를 사용하고
 * - Service 레이어에서 passwordEncoder.matches()로 비밀번호를 검증합니다.
 * 
 * @deprecated 비밀번호 암호화로 인해 사용하지 않음. findByUsernameWithRoles() 사용 권장
 */
@Deprecated
Optional<User> findByUsernameAndPassword(String username, String password);
```

---

## 💡 학생들을 위한 개념 설명

### 1. 비밀번호 암호화가 왜 필요한가요?

**문제 상황**:
- 평문 비밀번호를 그대로 저장하면 데이터베이스가 해킹당했을 때 모든 사용자의 비밀번호가 노출됩니다.
- 사용자가 여러 사이트에서 같은 비밀번호를 사용하는 경우, 한 곳이 해킹당하면 다른 사이트도 위험해집니다.

**해결 방법**:
- 비밀번호를 암호화(해싱)하여 저장합니다.
- BCrypt는 단방향 해싱 알고리즘이므로 복호화가 불가능합니다.
- 사용자가 입력한 비밀번호를 암호화하여 저장된 값과 비교합니다.

---

### 2. BCrypt란?

**BCrypt**는 비밀번호 해싱을 위한 암호화 알고리즘입니다.

**특징**:
- **단방향 해싱**: 암호화는 가능하지만 복호화는 불가능
- **Salt 자동 생성**: 같은 비밀번호를 암호화해도 매번 다른 해시값 생성
- **안전한 검증**: `matches()` 메서드로 안전하게 비교

**예시**:
```java
// 같은 비밀번호 "1234"를 암호화해도 매번 다른 해시값 생성
String hash1 = passwordEncoder.encode("1234");
// 결과: $2a$10$N9qo8uLOickgx2ZMRZoMyeIjZAgcfl7p92ldGxad68LJZdL17lhWy

String hash2 = passwordEncoder.encode("1234");
// 결과: $2a$10$abcdefghijklmnopqrstuvwxyz1234567890ABCDEFGHIJKLMNOPQRST
// → hash1과 hash2는 완전히 다름!
```

---

### 3. encode()와 matches()의 차이

**encode()**:
- 평문 비밀번호를 암호화된 해시값으로 변환
- 회원가입, 비밀번호 변경 시 사용
- 매번 다른 해시값 생성 (Salt 자동 생성)

**matches()**:
- 평문 비밀번호와 암호화된 비밀번호를 비교
- 로그인 시 사용
- BCrypt가 자동으로 Salt를 추출하여 비교

**예시**:
```java
// 회원가입 시
String hashPwd = passwordEncoder.encode("1234");
user.setPassword(hashPwd); // DB에 저장

// 로그인 시
String inputPwd = "1234"; // 사용자가 입력한 비밀번호
String savedPwd = user.getPassword(); // DB에 저장된 암호화된 비밀번호

boolean isMatch = passwordEncoder.matches(inputPwd, savedPwd);
// → true (비밀번호 일치)
```

---

### 4. 왜 DB에서 비밀번호를 직접 비교하지 않나요?

**문제**:
- BCrypt로 암호화된 비밀번호는 평문과 직접 비교할 수 없습니다.
- 같은 비밀번호를 암호화해도 매번 다른 해시값이 생성됩니다 (Salt 때문).

**해결**:
- DB에서는 사용자명만으로 사용자를 조회합니다.
- 애플리케이션에서 `passwordEncoder.matches()`로 비밀번호를 검증합니다.
- BCrypt가 자동으로 Salt를 추출하여 비교합니다.

**예시**:
```java
// ❌ 잘못된 방법 (DB에서 직접 비교)
// SELECT * FROM user_tb WHERE username = ? AND password = ?
// → 암호화된 비밀번호와 평문 비밀번호를 직접 비교할 수 없음

// ✅ 올바른 방법 (애플리케이션에서 검증)
User user = userRepository.findByUsernameWithRoles(username);
if (passwordEncoder.matches(inputPassword, user.getPassword())) {
    // 로그인 성공
}
```

---

### 5. Salt란?

**Salt**는 비밀번호 해싱 시 추가되는 랜덤 문자열입니다.

**역할**:
- 같은 비밀번호를 암호화해도 매번 다른 해시값을 생성
- 레인보우 테이블 공격 방지

**예시**:
```
비밀번호: "1234"
Salt 1: "abc123" → 해시값: $2a$10$...abc123...
Salt 2: "xyz789" → 해시값: $2a$10$...xyz789...
→ 같은 비밀번호지만 해시값이 다름!
```

**BCrypt의 특징**:
- Salt를 자동으로 생성하고 해시값에 포함시킵니다.
- 개발자가 직접 Salt를 관리할 필요가 없습니다.
- `matches()` 메서드가 자동으로 Salt를 추출하여 비교합니다.

---

### 6. 보안 강화 효과

**이전 (평문 저장)**:
- 데이터베이스 해킹 시 모든 비밀번호 노출
- 사용자가 여러 사이트에서 같은 비밀번호 사용 시 연쇄 피해

**현재 (BCrypt 암호화)**:
- 데이터베이스 해킹 시에도 비밀번호 복호화 불가능
- 각 사이트마다 다른 해시값 생성 (Salt 때문)
- 안전한 비밀번호 검증

---

## 주요 파일 변경 목록

1. **`build.gradle`** (수정)
   - `spring-security-crypto` 의존성 추가
   
2. **`WebMvcConfig.java`** (수정)
   - `PasswordEncoder` 빈 등록 메서드 추가
   
3. **`UserService.java`** (수정)
   - `PasswordEncoder` 의존성 주입
   - 회원가입 시 비밀번호 암호화 처리
   - 로그인 시 비밀번호 검증 로직 수정
   - 회원정보 수정 시 비밀번호 암호화 처리
   
4. **`UserRepository.java`** (수정)
   - `findByUsernameWithRoles()` 메서드 수정 (비밀번호 조건 제거)
   - `findByUsernameAndPassword()` 메서드에 `@Deprecated` 추가

5. **`UserController.java`** (수정)
   - `PasswordEncoder` 의존성 주입
   - 카카오 소셜 로그인 최초 가입 시 임시 비밀번호(Bcrypt) 해싱 처리

---

## 🎯 핵심 정리

### 비밀번호 암호화의 핵심

1. **BCrypt 알고리즘**: 단방향 해싱으로 복호화 불가능
2. **Salt 자동 생성**: 같은 비밀번호도 매번 다른 해시값 생성
3. **안전한 검증**: `matches()` 메서드로 안전하게 비교
4. **보안 강화**: 데이터베이스 해킹 시에도 비밀번호 보호

### 실무에서 중요한 점

- **의존성 관리**: Spring Security 전체가 아닌 Crypto 모듈만 사용
- **빈 등록**: PasswordEncoder를 빈으로 등록하여 어디서든 사용 가능
- **비밀번호 검증**: DB에서 직접 비교하지 않고 애플리케이션에서 검증
- **기존 사용자 처리**: 기존 사용자의 비밀번호는 회원정보 수정 시 자동으로 암호화됨

---

**작성일**: 2026년
**프로젝트**: Spring Blog (SSR v1.1)
